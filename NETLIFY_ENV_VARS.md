# Environment Variables (Current)

Source of truth: `client/.env.local.example`

## Required groups
- `NEXT_PUBLIC_FIREBASE_*`
- `FIREBASE_SERVICE_ACCOUNT` (or split admin vars)
- Optional Cloudinary vars for `/api/upload`

This project’s primary deployment target is Vercel.
