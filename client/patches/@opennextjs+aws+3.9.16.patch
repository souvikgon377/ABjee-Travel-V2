diff --git a/node_modules/@opennextjs/aws/dist/build/copyTracedFiles.js b/node_modules/@opennextjs/aws/dist/build/copyTracedFiles.js
index 818e026..b770060 100644
--- a/node_modules/@opennextjs/aws/dist/build/copyTracedFiles.js
+++ b/node_modules/@opennextjs/aws/dist/build/copyTracedFiles.js
@@ -211,9 +211,21 @@ File ${serverPath} does not exist
                 symlinkSync(symlink, to);
             }
             catch (e) {
-                if (e.code !== "EEXIST") {
-                    throw e;
+                if (e.code === "EEXIST") {
+                    return;
                 }
+                // Windows can block symlink creation (EPERM) depending on permissions.
+                // Fall back to copying so local builds can proceed.
+                if (e.code === "EPERM" || e.code === "ENOTSUP") {
+                    try {
+                        cpSync(from, to, { recursive: true, force: true, dereference: true });
+                        return;
+                    }
+                    catch {
+                        // If copy fallback also fails, surface the original symlink error.
+                    }
+                }
+                throw e;
             }
         }
         else {
