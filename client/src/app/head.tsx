// Head links and third-party loaders are intentionally managed by the root layout.
// This compatibility component intentionally has no environment-dependent logic.
export default function Head() {
  return null;
}
